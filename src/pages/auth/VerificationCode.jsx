import { Box, Typography } from "@mui/material";

const VerificationCode = () => {
  return (
    <Box>
      <Typography>this is verfication code</Typography>
    </Box>
  );
};

export default VerificationCode;
